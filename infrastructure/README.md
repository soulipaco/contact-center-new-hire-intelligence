# Infrastructure

`databricks/resources/` contains the declarative bundle resources:

- four core data/ML workflows
- one optional action-intelligence workflow
- one AI/BI dashboard
- one native Genie space

`environments/` contains credential-free examples only. Authentication is provided
by a Databricks CLI OAuth profile; tokens and local environment files are ignored.
