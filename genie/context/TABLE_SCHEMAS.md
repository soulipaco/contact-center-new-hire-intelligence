# Table Schemas

The deployable column contract is maintained in `genie/source/metadata/columns/`.
The five files in that directory are the source of truth for Genie-visible fields,
entity matching, formatting assistance, and exclusions.
